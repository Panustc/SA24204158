% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_knn}
\alias{benchmark_knn}
\title{Benchmark KNN Functions (R vs C)}
\usage{
benchmark_knn(train_data, train_labels, test_data, k)
}
\arguments{
\item{train_data}{A numeric matrix where rows are training samples and columns are features.}

\item{train_labels}{An integer vector representing the labels for the training data.}

\item{test_data}{A numeric matrix where rows are test samples and columns are features.}

\item{k}{An integer specifying the number of nearest neighbors to consider.}
}
\value{
A \code{microbenchmark} object that contains the execution time of both functions.
}
\description{
Use the \code{microbenchmark} package to compare the performance of the C function \code{knnC} and the R function \code{knnR}.
}
\examples{
\dontrun{
# Example data
train_data <- matrix(c(1, 2, 1, 3, 4, 5, 6, 7), ncol = 2)
train_labels <- c(0, 0, 1, 1)
test_data <- matrix(c(1.5, 2.5, 6, 7), ncol = 2)
k <- 3
# Run the benchmark
benchmark_results <- benchmark_knn(train_data, train_labels, test_data, k)
print(benchmark_results)
}
}
