% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{tdnnCvC}
\alias{tdnnCvC}
\title{TDNN Classifier with Cross-Validation for s2}
\usage{
tdnnCvC(train_data, train_labels, s2_values, c)
}
\arguments{
\item{train_data}{A numeric matrix where rows are training samples and columns are features}

\item{train_labels}{An integer vector representing labels for the training data}

\item{s2_values}{A vector of integers representing the candidate values of s2 for cross-validation}

\item{c}{A scalar parameter that defines the relationship between s1 and s2. Specifically, s1 = c * s2.}
}
\value{
The optimal s2 value that maximizes the average cross-validation accuracy
}
\description{
A TDNN implementation that selects the best s2 using 5-fold cross-validation, with s1 = 2 * s2
}
\examples{
\dontrun{
train_data <- matrix(rnorm(200 * 2), ncol = 2)
train_labels <- sample(c(0, 1), size = 200, replace = TRUE)
s2_values <- c(1, 2, 3, 4, 5)
best_s2 <- tdnnCvC(train_data, train_labels, s2_values)
print(best_s2)
}
}
